//implements rounded dropdown menu
export function NameDropdown() {
    return (
        <select className="select select-bordered w-full max-w-m -mt-10 rounded-xl">
            <option disabled selected></option>
            <option>Placeholder</option>
        </select>
    )
}
